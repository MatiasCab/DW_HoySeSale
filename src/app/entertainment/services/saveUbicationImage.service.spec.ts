/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SaveUbicationImageService } from './saveUbicationImage.service';

describe('Service: SaveUbicationImage', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SaveUbicationImageService]
    });
  });

  it('should ...', inject([SaveUbicationImageService], (service: SaveUbicationImageService) => {
    expect(service).toBeTruthy();
  }));
});
